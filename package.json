{
  "name": "bc-concours-10ans",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Boxing Center — landing jeu concours 10 ans. Inscription + 2 ami(e)s + WhatsApp.",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "node server/dev-api.js --vite",
    "dev:web": "vite --port 5620 --open",
    "dev:api": "node server/dev-api.js",
    "build": "vite build",
    "preview": "vite preview --port 5620",
    "test": "node --test test/phone.test.js test/contest.test.js test/api.test.js",
    "test:e2e": "playwright test",
    "db:avis": "node scripts/ensure-avis-column.mjs"
  },
  "devDependencies": {
    "@playwright/test": "^1.55.0",
    "vite": "^5.4.10"
  }
}
